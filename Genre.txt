Music genre classification is composed of two basic steps: feature extraction and classification
-various features are extracted from the waveform
-a classifier is built using the features extracted from the training data